# LOCAL DEVELOPMENT REQUIREMENTS (Python 3.12 compatible)
# Use this for local development instead of requirements.txt
#
# Install with: pip install -r requirements-local.txt
# Or even better: pip install -e . (uses pyproject.toml)

# Build tools
build
wheel
setuptools>=80.0.0

# Core packages (Python 3.12 compatible versions)
boto3>=1.24.90
cryptography
pandas>=1.3.0
pyarrow>=10.0.0
pyjwt>=2.8.0
pytz
snowflake-connector-python>=2.7.0
usaddress>=0.3.0
requests>=2.25.0

streamlit>=1.28.0
pydantic>=2.0.0



# Note: This file is for LOCAL development only!
# For AWS Glue: Use the wheel installation method in pyproject.toml documentation
# For production: pip install --only-binary=:all: eimutils
