# Pip constraints file to avoid build issues with binary dependencies
# Use with: pip install -c constraints.txt -e .

--only-binary=pyarrow
--only-binary=pandas  
--only-binary=numpy
--only-binary=scipy
--only-binary=:all:

# Specific versions known to work well together
pyarrow>=14.0.0,<18.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
